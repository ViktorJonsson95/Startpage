import RSS from "./RSS";
import RSSSettings from "./RSSSettings";

const manifest = {
    id: "rss",

    name: "RSS",

    defaultWidth: 2,

    defaultHeight: 2,

    defaultTheme: "card",

    component: RSS,
    settingsComponent: RSSSettings,
};

export default manifest;