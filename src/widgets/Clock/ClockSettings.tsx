
type ClockSettingsProps = {
    widgetId: string;
};

const ClockSettings = ({
    widgetId,
}: ClockSettingsProps) => {
    return (
        <div>ClockSettings{widgetId}</div>
    )
}

export default ClockSettings