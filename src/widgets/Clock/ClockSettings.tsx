import React from 'react'
type ClockSettingsProps = {
    widgetId: string;
};

const ClockSettings = ({
    widgetId,
}: ClockSettingsProps) => {
    return (
        <div>ClockSettings</div>
    )
}

export default ClockSettings